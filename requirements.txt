# Pinned to the versions used for the paper experiments.
# JAX/jaxlib must match your CUDA version; the lines below are CUDA 12.x defaults.

jax==0.4.30
jaxlib==0.4.30
chex==0.1.86
optax==0.2.3
flax==0.8.5
dm-haiku==0.0.13
mctx==0.0.5

# Environments
pgx==2.4.0

# Jumanji vendored under committed_action/; its own runtime deps:
hydra-core>=1.3
omegaconf>=2.3
matplotlib>=3.7
pillow>=9
numpy>=1.24
scipy>=1.11
typing_extensions>=4.5
pydantic>=2

# Training utilities
tqdm>=4.65
wandb>=0.16

# Optional download helpers (for scripts/download_checkpoints.sh)
gdown>=5.1
