defaults:
    - _self_
    - env: sokoban  # [bin_pack, cleaner, connector, cvrp, flat_pack, game_2048, graph_coloring, job_shop, knapsack, maze, minesweeper, mmst, multi_cvrp, pac_man, robot_warehouse, lbf, rubiks_cube, search_and_rescue, sliding_tile_puzzle, snake, sokoban, sudoku, tetris, tsp]

agent: gumbel_alphazero  # [random, a2c, gumbel_alphazero]

seed: 0

logger:
    type: terminal  # [neptune, tensorboard, terminal, wandb]
    save_checkpoint: true  # [false, true]
    checkpoint_file_name: training_state
    checkpoint_dir: checkpoints
    name: ${agent}_${env.name}
    project: jumanji
    entity: null

